name: Pull Request Labeler
on:
  - pull_request_target

jobs:
  labeler:
    name: Label PRs
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - name: Label PRs
      uses: actions/labeler@v6
      with:
        sync-labels: true
